<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Iniciar Sesión</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-dark text-white">
  <div class="container d-flex align-items-center justify-content-center min-vh-100">
    <div class="card p-4 shadow-sm" style="max-width: 400px; width: 100%; background-color: #1a1a1a; border: none;">
      <div class="card-body text-center">
        <img src="/img/totoro.png" class="rounded-circle mb-3" alt="User Icon" width="100" height="100">
        <h3 class="mb-4">Iniciar Sesión</h3>
        {{#if failLogin}}
          <div class="alert alert-danger" role="alert">
            Inicio de sesión fallido. Por favor, verifica tus credenciales e intenta nuevamente.
          </div>
        {{/if}}
        <form action="/api/sessions/login" method="POST">
          <div class="form-group mb-3">
            <label for="email" class="form-label sr-only">Correo electrónico</label>
            <div class="input-group">
              <div class="input-group-text"><i class="fa fa-envelope"></i></div>
              <input type="email" class="form-control" id="email" name="email" placeholder="Correo electrónico" required>
            </div>
          </div>
          <div class="form-group mb-4">
            <label for="password" class="form-label sr-only">Contraseña</label>
            <div class="input-group">
              <div class="input-group-text"><i class="fa fa-lock"></i></div>
              <input type="password" class="form-control" id="password" name="password" placeholder="Contraseña" required>
            </div>
          </div>
          <a href="#" class="text-white mb-3 d-block">Forgot your password?</a>
          <button type="submit" class="btn btn-block mb-3" style="background-color: #ff69b4; color: #fff;">Sign In</button>
          <a href="/auth/github" class="btn btn-dark btn-block mb-3">
            <i class="fab fa-github"></i> Iniciar sesión con GitHub
          </a>
        </form>
        <p>Don't have an account? <a href="/register" class="text-white">REGISTER</a></p>
      </div>
    </div>
  </div>
  <!-- Scripts necesarios -->
  <script src="https://kit.fontawesome.com/4e9044019f.js" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
